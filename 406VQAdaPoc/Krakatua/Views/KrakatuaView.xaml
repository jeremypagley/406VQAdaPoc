<UserControl x:Class="_406VQAdaPoc.Krakatua.Views.KrakatuaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="593">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="FontSize" Value="30" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="HorizontalAlignment" Value="Center" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="205" />
            <RowDefinition Height="35" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        
        <Image Height="130"
               Width="240"
               Opacity="0.8"
               Grid.Row="0"
               Margin="5,-15,5,5"
               Stretch="Fill"
               VerticalAlignment="Top" 
               Name="KrakatauLogo"
               Source="/406VQAdaPoc;component/Assets/Graphics/logo_krakatau.png" />
        
        <Label HorizontalAlignment="Center"
               VerticalAlignment="Top"
               Content="KRAKATAU AQUA COASTER"
               Grid.Row="1" />
        <TextBlock Grid.Row="1"
                   Foreground="White"
                   Margin="0,40,0,0"
                   Width="480"
                   Height="120"
                   Padding="20"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="{Binding SelectedAttraction.Script}">
            <TextBlock.Background>
                <ImageBrush ImageSource="/406VQAdaPoc;component/Assets/Graphics/Full_Width_Button.png" />
            </TextBlock.Background>
        </TextBlock>

        <Label Content="Your return time is:"
               Grid.Row="3"
               FontSize="16"
               VerticalAlignment="Top" />
        <Label Content="{Binding SelectedAttraction.ReturnTime}"
               ContentStringFormat="{}{0} minutes"
               Grid.Row="3"
               FontSize="54"
               VerticalAlignment="Bottom" />
    </Grid>
</UserControl>
